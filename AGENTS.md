# AGENTS.md

## 当前状态

本文件是项目协作与自动化执行规则的初始模板。目前尚未定义项目专属的实施方案、架构或服务器操作规则。

## 通用工作方式

- 开始变更前，确认当前工作目录并查看 `git status`，保护已有的未提交改动。
- 先阅读与当前任务直接相关的代码、配置和文档，再实施最小必要改动。
- 不还原、覆盖或删除不属于当前任务的文件和他人改动。
- 新增或修改行为时，执行与改动相匹配的测试；无法执行时说明原因和影响。

## 安全与敏感信息

- 不读取、打印、记录或提交真实的 Token、密码、私钥、`.env` 内容及其他密钥。
- 只提交无敏感值的配置模板，例如 `.env.example`。
- 涉及外部服务、生产环境、权限、网络或费用的操作，先取得项目负责人确认。

## 只读参考目录边界

- `no_training_vlm_retrieval/` 仅作为图像检索增强 VLM 功能的只读参考资料，开发期间不得修改该目录内的文件或代码。
- 正式实现必须按照本项目架构落入主项目目录，不得从该目录导入模块、建立路径或包依赖、调用其中脚本，或依赖其中的配置、提示词及运行时资源。
- 测试、构建、部署和服务启动均不得要求该目录存在；开发完成后该目录会被删除。
- 相关功能的验收必须包含删除或隔离该目录后的验证，确保主系统仍可独立安装、测试、启动和运行。

## Git 与交付

- 提交前检查待提交文件，确保不包含本地媒体、构建产物、虚拟环境、回退归档或敏感配置。
- 保持提交范围聚焦，并在提交说明中概括变更目的。
- 重要架构、依赖、持久化、网络或安全边界变更应记录为可追溯的决策文档。

## 后续维护

- 新方案确定后，在本文件中补充其任务边界、验证要求、部署约束和必要的授权流程。
