# 关键画面检测方案
> 状态：方案已采纳，待实施。
>
> 目标：长时视频中，只有当关键要素新出现、数量或状态发生明显变化，或者海底场景发生较大变化时，才触发截图、视觉文本大模型识别和画面讲解。相同或近似场景不能持续重复描述。

## 1. 方案结论

采用“YOLO 目标检测与跟踪 + 场景变化检测 + 异步视觉文本大模型”的事件驱动方案：

~~~text
摄像头连续采样 JPEG
  -> YOLO 检测已知目标
  -> ByteTrack 跟踪目标状态
  -> 场景变化检测覆盖底质、微地形和未知变化
  -> 产生候选事件并立即截取 JPEG
  -> 异步调用视觉文本大模型
  -> 确认调查价值并生成一次画面讲解
  -> 保存正式截图和事件记录
~~~

本方案不再使用短 MP4，不再使用文本向量模型，也不再根据文本描述相似度去重。

## 2. 关键帧的定义

本系统中的关键帧不是“画面质量最好的一帧”，而是“关键要素状态发生变化时的事件帧”。

关键帧的触发条件包括：

- 新生物或其他目标进入画面；
- 已有目标数量明显变化；
- 已有目标状态发生值得调查的变化；
- 海底底质发生明显变化；
- 出现新的微地形或微地貌；
- 画面进入新的海底场景；
- 出现其他具有调查价值的未知要素。

清晰度检测只能用于排除完全不可用的帧，不能作为选取关键画面的主要依据。

## 3. YOLO 的能力边界

YOLO 负责快速发现已知类别目标，适合处理：

- 已知生物类别的出现和消失；
- 目标数量变化；
- 目标位置和面积变化；
- 新目标进入画面；
- 配合跟踪器区分“同一目标持续移动”和“新目标出现”。

YOLO 不能独立完成全部需求：

- 未训练过的新生物可能无法检测；
- 底质和微地形通常不是边界稳定的目标框；
- 开放类别的“其他调查价值要素”无法完全依靠固定类别检测；
- YOLO 不能独立判断变化是否具有调查价值。

因此，YOLO 是候选事件检测器，不是最终判定器。第一版可以使用 Ultralytics YOLO 的轻量模型和 ByteTrack；没有深海专用权重时，YOLO 只覆盖其可识别的已知目标，未知变化由场景变化检测和视觉文本大模型补充。

## 4. 推荐技术栈

| 环节 | 技术 | 作用 |
| --- | --- | --- |
| 摄像头采集 | 浏览器 getUserMedia、Canvas | 持续采样 JPEG |
| 帧上传 | multipart/form-data、Flask | 传输最新画面，不上传原始视频流 |
| 已知目标检测 | Ultralytics YOLO | 检测生物及其他已知目标 |
| 目标跟踪 | ByteTrack | 为目标分配轨迹 ID，避免同一目标重复触发 |
| 场景变化检测 | OpenCV、NumPy | 检测底质、微地形和大范围场景变化 |
| 最终语义判断 | Qwen3-VL | 判断新要素和调查价值，生成中文描述 |
| 异步调度 | 单任务工作队列 | 适配大模型 2-4 秒推理时间 |
| 持久化 | 文件系统、SQLite | 保存正式截图和事件元数据 |

## 5. 候选事件检测

系统同时运行两类候选检测器。任意一类确认发生有效变化，都可以生成候选事件。

### 5.1 YOLO 目标事件检测

YOLO 对采样帧持续执行目标检测，ByteTrack 为检测结果分配稳定的轨迹 ID。每个轨迹至少记录：

~~~text
track_id
category
confidence
bounding_box
first_seen_time
last_seen_time
continuous_frames
~~~

以下情况可以触发候选事件：

- 出现新的稳定 track_id；
- 某一类别的目标数量明显增加；
- 目标面积、状态或组合关系发生明显变化；
- 目标连续存在达到确认帧数，排除单帧误检。

以下情况不触发新事件：

- 同一个 track_id 只是在画面中持续移动；
- 目标框发生小幅抖动；
- 低置信度目标只出现一帧；
- 已记录目标仍处于同一事件场景中。

### 5.2 场景变化事件检测

对于海底底质、微地形、微地貌和未知目标，使用当前帧与场景参考帧进行视觉比较。建议组合：

- 感知哈希距离，用于判断整体结构变化；
- HSV 颜色和纹理分布，用于判断底质变化；
- 边缘和轮廓分布，用于判断裂缝、沟槽、陡坎等结构变化；
- 局部网格差异，用于发现只占画面小区域的新要素；
- 连续帧确认，用于过滤抖动、悬浮颗粒和瞬时光照变化。

场景变化检测只负责判断“可能进入了新的视觉状态”，最终是否具有调查价值由视觉文本大模型确认。

### 5.3 候选事件合并

同一时间窗口内，YOLO 目标事件和场景变化事件应合并为一个候选事件，避免针对同一画面调用多次大模型。

候选事件至少包含：

~~~text
candidate_id
session_id
captured_at
current_image_path
reference_image_path
yolo_changes
scene_change_metrics
trigger_type
~~~

候选 JPEG 应在触发时立即保存到候选目录，防止大模型推理期间丢失真正发生变化的画面。

## 6. 视觉文本大模型处理

### 6.1 模型输入

不编码短 MP4。模型只接收图片和结构化检测信息：

- 上一次场景参考 JPEG；
- 当前候选 JPEG；
- YOLO 检测到的新增目标、数量变化和轨迹信息；
- 场景变化检测指标；
- 当前会话已经确认的调查要素摘要。

建议新增接口：

~~~text
POST /v1/vision/evaluate-survey-event
~~~

### 6.2 模型输出

建议返回固定 JSON：

~~~json
{
  "survey_value": true,
  "event_type": "new_element",
  "scene_changed": true,
  "new_elements": [
    {
      "category": "organism",
      "name": "深海虾",
      "is_new": true
    }
  ],
  "description": "画面右侧新出现一只深海虾，正在碎屑质海底表面缓慢移动。",
  "confidence": 0.91
}
~~~

要素类别固定为：

~~~text
organism
seabed_substrate
micro_topography
other
~~~

事件类型固定为：

~~~text
new_element
major_scene_change
none
~~~

模型只负责确认调查价值、识别新要素并生成描述，不参与文本向量去重。

## 7. 大模型延迟和异步队列

视觉文本大模型完成一次判断需要约 2-4 秒，因此模型调用必须与摄像头采集、YOLO 检测和跟踪完全解耦。

每个会话采用以下队列限制：

~~~text
最多 1 个正在处理的模型任务
最多 1 个等待处理的候选事件
~~~

模型处理期间：

- 摄像头继续采样；
- YOLO 和 ByteTrack 继续运行；
- 相似候选合并到正在处理的事件，不重复排队；
- 出现明显不同的新候选时，用最新候选更新等待槽；
- 模型完成后，再处理等待槽中的候选；
- 不允许候选事件无限积压。

候选合并依据视觉状态、YOLO 轨迹 ID、触发类型和时间窗口，不使用文本向量。

## 8. 事件状态机

每个会话使用以下状态：

~~~text
MONITORING
  -> YOLO 或场景检测确认变化
CANDIDATE_PENDING
  -> 提交异步模型任务
MODEL_EVALUATING
  -> 模型判定具有调查价值
EVENT_ACCEPTED
  -> 画面离开当前事件场景
MONITORING
~~~

需要维护：

~~~text
active_tracks
last_scene_reference
last_accepted_frame
active_event_signature
model_task_in_flight
pending_candidate
last_model_call_time
~~~

active_event_signature 由 YOLO 轨迹状态、目标数量、视觉指纹和触发类型组成，用于确定性地合并重复候选。

## 9. 正式事件触发和保存

只有同时满足以下条件，才接受正式事件：

~~~text
YOLO 或场景变化检测确认候选变化
且模型判断 survey_value = true
且模型判断存在新要素或重大场景变化
且当前候选不属于已锁定的同一事件状态
~~~

通过后，将候选 JPEG 转为正式调查截图并保存到：

~~~text
data/captures/<session_id>/<timestamp>_<uuid>.jpg
~~~

SQLite 保存：

~~~text
event_id
session_id
event_time
event_type
trigger_type
element_category
element_name
description
confidence
image_path
yolo_track_ids
visual_fingerprint
~~~

模型拒绝的候选可以删除，或者保留短期诊断信息用于调整 YOLO 和场景变化阈值。

## 10. 明确停用的功能

监测链路停用：

- MP4 重组和短视频上传；
- embedding.embed([content])；
- last_memo_embedding；
- memo_similarity_threshold；
- 监测专用 memo_embedding 文本向量模型；
- 基于中文描述相似度的重复判断。

RAG 文档检索使用的 rag_embedding 与监测链路独立。若只停用监测文本向量模型，RAG 可以保留；若停用全部文本向量模型，则 RAG 需要另行设计。

## 11. 需要调整的代码模块

- 前端继续上传 JPEG，不改变为视频流或 MP4；
- VideoIngestionService 改为帧接收和候选事件输入，不再默认生成 MP4；
- 新增 YOLO 检测服务；
- 新增 ByteTrack 跟踪状态管理；
- 新增场景变化检测服务；
- 新增候选事件合并器；
- 新增每会话单任务异步模型队列；
- MonitoringService 改为事件驱动，不再定时描述最新视频；
- 视觉模型网关增加双 JPEG 和结构化元数据接口；
- 新增截图和事件持久化仓储；
- ApplicationContainer 移除监测专用 memo_embedding；
- Settings 停用 MEMO_EMBEDDING_MODEL_PATH 和 MEMO_SIMILARITY_THRESHOLD。

## 12. 验收标准

1. 同一海底画面持续观察 5 分钟，不持续重复描述。
2. 同一只生物持续移动时，ByteTrack 保持同一轨迹，不重复生成事件。
3. 新生物进入画面并稳定出现后，生成一次候选事件和正式截图。
4. 生物数量明显变化时，生成一次候选事件。
5. 底质明显变化时，即使 YOLO 没有检测框，也能通过场景变化检测触发候选。
6. 出现沟槽、裂隙、陡坎等微地貌时，能够触发候选并交给大模型确认。
7. 短暂抖动、光照闪动、悬浮颗粒和单帧误检不产生正式事件。
8. 大模型处理 2-4 秒期间，摄像头采集和 YOLO 跟踪不中断。
9. 每个会话最多保留一个在途任务和一个等待候选，不产生无限任务积压。
10. 全流程不生成短 MP4，不调用监测文本向量模型。

## 13. 最终处理路径

~~~text
浏览器采样并上传 JPEG
  -> YOLO 检测已知目标
  -> ByteTrack 判断目标是否新出现或发生明显变化
  -> OpenCV 判断底质、微地形或整体场景是否明显变化
  -> 合并为一个候选事件并立即截取 JPEG
  -> 异步提交参考 JPEG、候选 JPEG 和检测元数据
  -> Qwen3-VL 确认调查价值并生成结构化中文描述
  -> 保存正式截图和事件记录
~~~

该方案将系统从“按时间持续描述”改为“关键要素变化驱动描述”，并通过目标跟踪、场景变化检测和异步候选合并控制重复事件与大模型延迟。
